import Foundation

enum NavigationKey {
    static let title = "navigation.title"
}

enum ItemKey {
    static let title = "item.title"
    static let description = "item.description"
    static let edit = "item.edit"
}

enum MainViewKey {
    static let todoImage = "list.bullet"
    static let aboutImage = "questionmark.circle.fill"
}

enum TabKey {
    static let todo = "Todo"
    static let about = "About"
}
